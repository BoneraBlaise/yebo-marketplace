/** Prompt template stub — architecture only, no real prompts. */
export const shoppingTemplate = {
  id: "shopping",
  category: "shopping",
  description: "General shopping assistance",
  variables: ["query", "budget", "category", "region"],
  template: null,
};

export default shoppingTemplate;
