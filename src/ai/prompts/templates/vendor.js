export const vendorTemplate = {
  id: "vendor",
  category: "vendor",
  description: "Vendor business insight prompts",
  variables: ["sellerId", "metrics", "inventory"],
  template: null,
};

export default vendorTemplate;
