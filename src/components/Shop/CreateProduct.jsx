import React from "react";
import CreateProductWizard from "../seller-experience/CreateProductWizard";

/** @deprecated Use CreateProductWizard — kept for backward compatibility */
const CreateProduct = () => <CreateProductWizard />;

export default CreateProduct;
